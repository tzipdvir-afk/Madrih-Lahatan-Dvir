# פרויקט: "המדריך לחתן" — אתר הדרכת חתנים

מסמך זה הוא תקציר מסירה (handoff) מעבודה שנעשתה מול קלוד בצ'אט (claude.ai).
שים לב: זהו קובץ `CLAUDE.md` — Claude Code טוען אותו אוטומטית בתחילת כל
session כשפותחים אותו בתוך התיקייה הזו, כך שאין צורך להדביק את התוכן ידנית.

## מה זה בכלל

אתר-מדריך לחתנים לפני חתונה, בעברית, RTL, מבוסס HTML/CSS/JS סטטי (בלי
framework, בלי build step). מיועד לשימוש בנייד בעיקר. דף בית עם כרטיסי
נושאים, כל כרטיס מוביל לדף נושא נפרד שמתחיל בסרטון פתיחה ואחריו תוכן הלכתי
ו/או כלים אינטראקטיביים.

## מבנה התיקייה

```
site/
  index.html                 ← דף הבית, 5 כרטיסי נושאים
  pages/
    ketamim.html             ← הלכות כתמים (הכי מפותח, כמעט גמור)
    tahara-cycle.html        ← כלי משולב: מעגל הטהרה + הלכות וסתות (2 טאבים)
    harchakot.html           ← דיני הרחקות — רק וידאו, אין עדיין תוכן הלכתי
  media/
    videos/
      ketamim.mp4
      tahara-cycle.mp4
      vesatot.mp4
      harchakot.mp4
    images/
      home-ketamim.jpeg
      home-tahara-cycle.jpeg
      home-vesatot.jpeg
      home-harchakot.jpeg
```

כל הסרטונים: H.264/AAC, 720×1280 (פורטרט), כ-10 שניות כל אחד.

## החלטה טכנית קריטית — איך וידאו נטען (אל תסטה מזה!)

**בהיסטוריה של הפרויקט, וידאו הוטמע כ-base64 בתוך ה-HTML עצמו (data URI /
Blob URL). זה גרם לבאג עקבי: הסרטון לא התנגן, בלי שגיאה ברורה, לא במחשב
ולא בנייד.** הפתרון שעבד: **קובצי mp4 אמיתיים ונפרדים**, מקושרים בנתיב
יחסי רגיל:

```html
<video controls preload="metadata" playsinline>
  <source src="../media/videos/שם-הקובץ.mp4" type="video/mp4">
  הדפדפן שלכם לא תומך בהצגת וידאו מוטמע.
</video>
```

**לעולם אל תחזור להטמעת base64 של וידאו בקובץ HTML** — גם אם זה נראה
"נוח" לקובץ בודד. זה שבור באופן שקשה לאבחן.

## סטטוס לפי דף

### `index.html` — גמור
5 כרטיסים: מעגל הטהרה, הלכות כתמים, תהליך הטהרה (⏳ "בקרוב", לא-לחיץ,
עדיין אין לו תמונה/וידאו/תוכן), הלכות וסתות, דיני הרחקות. עיצוב: כחול-כהה
(navy) + זהב, פונטים Frank Ruhl Libre + Heebo.

### `pages/ketamim.html` — כמעט גמור
- וידאו תוקן (עבר מ-base64 לקובץ חיצוני).
- **עדיין ממתין**: המשתמש ציין שיש "תיקוני טקסט משמעותיים" לעשות אבל עוד
  לא פירט מה בדיוק — יש לשאול אותו.
- עיצוב: תכלת-זהב (סכימת צבעים ישנה יותר, **שונה** מהעיצוב navy/gold
  שבשאר האתר). המשתמש נשאל אם לאחד עיצוב — טרם הוכרע.
- מכיל: הקדמה, 5 יחידות לימוד (אקורדיון), האחרונה כוללת שאלון אינטראקטיבי
  (wizard) לבדיקת כתם — לוגיקה: הרגשה → צבע → מיקום (פנימי/חיצוני) → ...
  שים לב שהסדר שונה מהמקורי: **צבע נשאל לפני מיקום**, כי אם הצבע לא-אדום
  זה כבר טהור בלי קשר למיקום. שינוי זה בוצע בכוונה, אל תחזיר לסדר הישן.

### `pages/tahara-cycle.html` — עובד, מבוסס על כלי קיים
זה במקור קובץ עצמאי בשם "מעגל הטהרה - כלי עזר להדרכה" — כלי אינטראקטיבי
עם 2 טאבים פנימיים (`tabMain`/`screenMain` ו-`tabOurs`/`screenOurs`).
הטאב השני נקרא במקור "מעגל הטהרה שלנו" ושונה כאן ל**"הלכות וסתות"**
(רק הכיתוב — ה-id-ים הפנימיים בקוד נשארו כמו שהיו, אל תשנה אותם בלי
צורך אמיתי כי זה עלול לשבור פונקציונליות קיימת).

נוסף מנגנון ניתוב לפי פרמטר URL:
- `tahara-cycle.html?topic=cycle` (ברירת מחדל) → מציג סרטון מעגל הטהרה,
  ואז פותח לטאב הראשון
- `tahara-cycle.html?topic=vesatot` → מציג סרטון וסתות, פותח לטאב השני

**חשוב טכנית**: כל הקוד עטוף ב-IIFE גדול אחד `(function(){ ... })();`.
הפונקציה `switchTab()` מוגדרת בתוך ה-IIFE הזה. פונקציית `initVideoIntro()`
**חייבת** להיות מוגדרת ונקראת מבפנים אותו scope (לא כ-IIFE נפרד בסוף
הקובץ) כדי שתוכל לקרוא ל-`switchTab()`. זו טעות שכבר נעשתה ותוקנה —
היזהר לא לחזור עליה.

### `pages/harchakot.html` — שלד בלבד
רק סרטון + הודעת "התוכן ההלכתי בדרך". אין תוכן הלכתי אמיתי עדיין.
מעצבן להשלים כשהמשתמש ישלח את הטקסט.

### נושא חסר לגמרי: "תהליך הטהרה"
אין לו כרגע תמונה, וידאו, או דף בכלל. יש לו רק placeholder בדף הבית.

## פריסה (Deployment) — עדיין לא בוצע

התוכנית שסוכמה עם המשתמש:
1. ריפו **חדש וייעודי** ב-GitHub (לא הריפו הקיים `Hadrahat_Hatanim_Dvir`)
2. Netlify מחובר ל-GitHub (לא Netlify Drop) — כדי שעדכונים יתפרסמו לבד
   בכל push
3. תוכן תיקיית `site/` (לא התיקייה עצמה כעטיפה) צריך לשבת בשורש הריפו —
   כלומר `index.html` צריך להיות ב-root, לא בתוך תת-תיקייה.

בשלב זה עוד לא נוצר ריפו ולא חובר Netlify. ייתכן שזה כבר בוצע על ידי
המשתמש בין השיחות — כדאי לשאול אותו מה הסטטוס הנוכחי.

## פערים פתוחים (לשאול את המשתמש)

- מה בדיוק ה"תיקונים המשמעותיים" בטקסט של `ketamim.html`?
- האם לאחד את הסגנון החזותי בין `ketamim.html` (תכלת-זהב) לשאר האתר
  (navy-זהב)?
- תוכן הלכתי לדף `harchakot.html` (יש רק וידאו כרגע)
- תמונה + וידאו + תוכן לנושא "תהליך הטהרה" (עדיין לא קיים בכלל)
- האם הריפו ב-GitHub וחיבור Netlify כבר בוצעו?

## הערות עבודה כלליות

- כל הקבצים HTML עצמאיים (בלי build step, בלי npm), RTL, `lang="he" dir="rtl"`.
- מספר וואטסאפ לכפתורי יצירת קשר: `972528990131`
- המשתמש מעדיף לאשר תוכניות/שינויים משמעותיים לפני ביצוע, ולא לקבל הצפת
  שאלות הבהרה — עדיף להניח הנחה סבירה, לציין אותה בקצרה, ולהמשיך.
- קהל היעד: חתנים לקראת חתונה, לומדים דרך מדריכי הדרכת חתנים.
